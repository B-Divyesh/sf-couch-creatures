const state = require('../state.cjs');
module.exports = async function (context, req) { const room = state.room(String(context.bindingData.room || '').toUpperCase()); if (!room) { context.res = { status: 404, body: { error: 'Room not found or expired.' } }; return; } if (req.method === 'POST') { const move = req.body || {}; if (!Number.isInteger(move.player) || move.player < 0 || move.player > 3 || ![-1, 1].includes(move.direction)) { context.res = { status: 400, body: { error: 'Invalid move.' } }; return; } room.moves.push({ player: move.player, direction: move.direction }); if (room.moves.length > 120) room.moves.splice(0, room.moves.length - 120); context.res = { status: 202, body: { ok: true } }; return; } const after = Math.max(0, Number(req.query.after) || 0); context.res = { body: { cursor: room.moves.length, moves: room.moves.slice(after) } }; };
